export const enum RoleType {
  ADMIN = 'ADMIN',
  USER = 'USER',
  MANAGER = 'MANAGER',
}

export const enum RolesColumns {
  ID = 'ID',
  Name = 'Name',
  Description = 'Description',
}

export enum RoleId {
  ADMIN = 1,
  MANAGER = 2,
  USER = 3,
}